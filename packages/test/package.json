{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:codegen": "playwright codegen http://localhost:3001/"
  },
  "dependencies": {
    "@packages/common": "link:../common",
    "monaco-editor-typescript-locales": "link:../monaco-util"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1"
  }
}
