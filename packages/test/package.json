{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:codegen": "npx playwright codegen http://localhost:3001/"
  },
  "dependencies": {
    "@packages/common": "link:../common",
    "monaco-editor-typescript-locales": "link:../monaco-util"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1"
  }
}
